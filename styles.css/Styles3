 body {
            background-image: url('https://example.com/path/to/your/background-image.jpg');
            background-size: cover;
            background-position: center;
            background-repeat: no-repeat;
            margin: 0;
            padding: 0;
            font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
            color: #ffffff;
            line-height: 1.6;
        }

        h1 {
            margin-top: 20px;
            color: #1abc9c; /* Темно-зеленый цвет */
        }

        form {
            margin-bottom: 20px;
        }

        label {
            color: #ffffff;
            margin-right: 10px;
        }

        select, input {
            padding: 8px;
            margin-right: 10px;
            border: 1px solid #ffffff;
            border-radius: 4px;
            background-color: #2c3e50; /* Темно-серый цвет */
            color: #ecf0f1; /* Светло-серый цвет */
        }

        button {
            padding: 8px 16px;
            background-color: #3498db; /* Голубой цвет */
            color: #ffffff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }

        button:hover {
            background-color: #2980b9; /* Темно-голубой цвет при наведении */
        }

        #gallery {
            display: flex;
            flex-wrap: wrap;
            justify-content: space-around;
            align-items: center;
            margin: 20px;
        }

        #gallery img {
            width: 300px;
            height: auto;
            margin: 10px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1); /* Тень для изображений */
            transition: transform 0.3s ease-in-out;
        }

        #gallery img:hover {
            transform: scale(1.1); /* Увеличение изображения при наведении */
        }
    